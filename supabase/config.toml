project_id = "gevqwporirhaekapftib"

[functions.generate-palette]
# SECURITY: JWT verification is required for production to prevent unauthorized access
verify_jwt = true

[functions.create-checkout]
# JWT verification is handled manually in the function code (see create-checkout/index.ts)
# Setting verify_jwt = false allows the function to run and perform its own verification
verify_jwt = false

[functions.stripe-webhook]
# Webhooks come directly from Stripe, not authenticated users
# Security is handled via webhook signature verification in the function
verify_jwt = false
